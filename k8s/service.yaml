apiVersion: v1
kind: Service
metadata:
  name: ca-wolves
  labels:
    app: ca-wolves
spec:
  type: ClusterIP
  selector:
    app: ca-wolves
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
